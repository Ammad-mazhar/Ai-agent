[phases.setup]
nixPkgs = ["nodejs", "google-chrome", "cacert"]

[phases.install]
cmds = ["npm install"]

[phases.build]
# This line is the "Secret Sauce" for Puppeteer 21.x
cmds = ["PUPPETEER_CACHE_DIR=/root/.cache/puppeteer npx puppeteer browsers install chrome"]

[start]
cmd = "node index.js"