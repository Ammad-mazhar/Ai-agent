{
  "name": "ai-agent",
  "version": "1.0.0",
  "description": "High-speed TRM Email Sniper",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "node -e \"if (process.env.PUPPETEER_SKIP_CHROMIUM_DOWNLOAD !== 'true') { require('child_process').execSync('npx puppeteer browsers install chrome', { stdio: 'inherit' }); }\""
  },
  "dependencies": {
  "axios": "^1.6.0",
  "dotenv": "^16.3.1",
  "imap": "^0.8.19",
  "mailparser": "^3.6.5",
  "nodemailer": "^6.9.7",
  "puppeteer": "^21.5.0"
}
}